{
  "name": "monagenceweb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx @tailwindcss/cli -i src/input.css -o css/style.raw.css && npx lightningcss --nesting --targets '>= 0.25%' --minify css/style.raw.css -o css/style.css && rm css/style.raw.css",
    "dev": "npx @tailwindcss/cli -i src/input.css -o css/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "lightningcss-cli": "^1.31.1",
    "tailwindcss": "^4.1.18"
  }
}
